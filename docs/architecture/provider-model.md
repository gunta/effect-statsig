# Provider Model

The provider model supports:

- runtime provider adapters (Node, Browser, React, Cloudflare, Expo)
- composition with buildtime overrides
- deterministic precedence for composed evaluation
