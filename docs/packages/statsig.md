# statsig

## Concepts

`@effect-statsig/statsig` provides shared parsing and models for runtime adapters.

## Runtime requirements

Requires Effect and schema decoding support.

## Setup

Parse config and user inputs before adapter initialization.

## Integration patterns

Use parsed config and user models as adapter input boundaries.

## Samples

- `examples/statsig-shared`

## API docs

Exports are available from `@effect-statsig/statsig` index.

## FAQ

If parsing fails, validate required fields such as `environment` and `userId`.
