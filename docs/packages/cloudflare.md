# cloudflare

## Concepts

`@effect-statsig/cloudflare` adapts core services to worker runtime behavior.

## Runtime requirements

Cloudflare Worker-compatible execution model.

## Setup

Initialize the layer with edge config and worker request user context.

## Integration patterns

Bind one scoped evaluation lifecycle per worker request path.

## Samples

- `examples/cloudflare-worker`

## API docs

Exports are available from `@effect-statsig/cloudflare` index.

## FAQ

Verify edge-safe dependency usage in worker environments.
